---
type: CatalogRequirement
id: UI-013
title: "Operate the system from the UI"
owner: srini
status: draft
area: UI
priority: must
---

The UI shall be able to run IBuildOS operations — validate and lint, run tests (`TT-009`), run AI agents, generate artifacts/scaffolds, run init/setup, and build or refresh the portal — so the system is fully operable from the UI, not only the CLI.
