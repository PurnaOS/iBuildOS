---
type: CatalogRequirement
id: TS-003
title: "Friendly schema dialect"
owner: srini
status: draft
area: TS
priority: must
---

A type definition shall declare `fields` (required, enum/`one_of`, `pattern`, scalar type, doc) and `relationships` (target type, `min`/`max`, doc) in readable YAML.
