---
type: FunctionalRequirement
id: FR-0001
title: Generic, data-driven type loader (Layer 1)
owner: srini
status: implemented
priority: must
links:
  derives_from: [/requirements/br-0001.md]
---

The linter shall load every type definition from `docs/types/*.md` at runtime,
resolve `extends`/`abstract`, and enforce the compiled dialect — hardcoding no
type but `ArtifactType`. Pointing `--types` at a different set must change
enforcement with no code change.
