---
type: BusinessRequirement
id: BR-0001
title: A deterministic, OKF-conformant traceability linter for the Requirement→Task→Code→Test chain
owner: srini
status: accepted
priority: must
---

iBuildOS needs a fast, deterministic linter that turns a pile of OKF markdown into
a checkable knowledge graph — validating the core lifecycle chain
**Requirement → Task → Code → Test** end-to-end, with zero hardcoded taxonomy, so
any project can define its own SDLC just by editing `docs/types/`.
