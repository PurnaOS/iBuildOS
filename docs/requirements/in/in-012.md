---
type: CatalogRequirement
id: IN-012
title: "Adoption as tracked work"
owner: srini
status: draft
area: IN
priority: must
---

The brownfield/adoption flow shall **scaffold the migration itself as IBuildOS artifacts** — an adoption Initiative with per-area epics and backfill/baseline-burndown tasks linked to owners and a target release — so the rollout is dogfooded and visible in progress (`PM-001`) and adoption (`PM-008`) reporting rather than tracked outside the repo. (principle 11)
