---
type: Task
id: TASK-0005
title: Implement the iBuild run autonomous work executor
owner: srini
status: done
priority: should
code:
  - src/core/run/**
  - src/cli.ts
links:
  implements: [/requirements/ag/ag-012.md, /requirements/ag/ag-011.md]
  verified_by: [/tests/test-run-cmd.md]
---

New `src/core/run/` module (select, prompt, harness, record, git, loop) plus
ChainConfig ready/claim/blocked vocabularies, `run:` config, and the
`iBuild run` CLI command: pick ready tasks by capability predicates, spawn the
configured harness, gate on validate + tests, write ARUN records, commit
locally per task, never push.
