# USER_GUIDE.md — superseded

This guide described the v0.5 CLI-first traceability linter. iBuildOS is
being rebuilt from a clean slate as v2 (see [README.md](README.md) and
[`docs/spec/SPEC.md`](docs/spec/SPEC.md)) — a desktop app, not a CLI-first
tool — so this guide no longer describes the current product.

The original v0.5 guide is preserved at branch
[`archive/v1-cli`](../../tree/archive/v1-cli) /
tag `v1-cli-archive`: `git show archive/v1-cli:USER_GUIDE.md`.

A v2 user guide will be written once there's a v2 product to guide someone
through — see `docs/spec/EXECUTION-PLAN.md` for the milestone sequence.
