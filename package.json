{
  "name": "ibuildos-monorepo",
  "version": "2.0.0-dev",
  "private": true,
  "description": "iBuildOS v2 — a desktop app where product people and AI coding agents build software together, over a git repo of OKF documents.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "turbo": "^2.3.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.12.0"
}
