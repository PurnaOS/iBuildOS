# @ibuildos/acp

ACP client layer: agent registry, capability negotiation, sessions, scoped fs/terminal, permission broker, transcripts (T-005).

**Status:** not yet implemented — scoped to **M3 (ACP layer)** in
`docs/spec/EXECUTION-PLAN.md`. This package exists as a workspace boundary
(per `docs/spec/TECH-STACK.md` §3) ahead of that milestone's work landing.
