# @ibuildos/bridge

ACP↔AG-UI bridge and the generative-UI component catalog (T-004).

**Status:** not yet implemented — scoped to **M5 (Conversation & breakdown)** in
`docs/spec/EXECUTION-PLAN.md`. This package exists as a workspace boundary
(per `docs/spec/TECH-STACK.md` §3) ahead of that milestone's work landing.
