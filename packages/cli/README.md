# @ibuildos/cli

The headless `ibuildos` CLI — a thin command surface over @ibuildos/engine (T-009, DEFAULTS #13).

**Status:** not yet implemented — scoped to **M1–M2 (Engine & CLI)** in
`docs/spec/EXECUTION-PLAN.md`. This package exists as a workspace boundary
(per `docs/spec/TECH-STACK.md` §3) ahead of that milestone's work landing.
